# Assignment 1 by Nitish Jha
# Python program that accepts a word from the user and reverse it

word = input("Enter a word :")
length = len(word)

for i in range(length-1,-1,-1):
    print(word[i],end="")
